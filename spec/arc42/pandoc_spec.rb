require 'spec_helper'

describe Arc42::Pandoc do
  it 'has a version number' do
    expect(Arc42::Pandoc::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
